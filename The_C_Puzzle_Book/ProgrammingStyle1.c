#include <stdio.h>
#define A
#define B
#define C
#define D
#deifne E
#define F
#define OTHER
#define DIGIT
#define ALPHA

int main() {
    while (A) { 
    if(B) continue; 
    C; 
    } 
    do { 
        if( IA) continue; 
        else B; 
        C; 
    } while(A); 
    
    if(A) 
    else 
        if(B) 
        else; 
    if(B) 
    else; 
    if(C) D; 
    else; 
    if(C) E; 
    else F; 
    while ( (c=qetchar ( ) ) I .. ''\n' ) { 
    if( c==' , ) continue; 
    if( c==',\t' ) continue; 
    if( c<'O' ) return(OTHER); 
    if( c<='9' ) return(DIGIT); 
    if( c<'a' ) return(OTHER); 
    if( c<='z' ) return(ALPHA); 
//(Programming Style 1.1) 
//(Programming Style 1.2) 
//(Programming Style 1.3) 
    return(OTHER); 
}
